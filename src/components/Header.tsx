// src/components/Header.tsx

import React from 'react';

const Header: React.FC = () => (
  <header>
    <h1>Vive Groups Blog</h1>
    <p>HomeGroup Downtown Sunnyvale</p>
    <nav>
      {/* Navigation links */}
    </nav>
  </header>
);

export default Header;
