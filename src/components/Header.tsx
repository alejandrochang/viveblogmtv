import React from 'react';

const Header: React.FC = () => (
  <header>
    <p>Vive Blog: Downtown Sunnyvale</p>
    <nav>
      {/* Navigation links */}
    </nav>
  </header>
);

export default Header;
